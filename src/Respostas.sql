--CREATE TABLE customers(id SERIAL PRIMARY KEY,"fullName" VARCHAR(50) NOT NULL, cpf VARCHAR(11) NOT NULL UNIQUE, email TEXT NOT NULL UNIQUE, password VARCHAR(20) NOT NULL);
--CREATE TABLE customersAdresses (id SERIAL PRIMARY KEY, "customerId" VARCHAR(10) NOT NULL,street VARCHAR(50) NOT NULL, number INTEGER NOT NULL, complement VARCHAR(50), "postalCode" VARCHAR(7), "cityId" VARCHAR(5))
--CREATE TABLE cities (id SERIAL PRIMARY KEY, name TEXT NOT NULL);
--CREATE TABLE states (id SERIAL PRIMARY KEY, name TEXT NOT NULL UNIQUE);
--CREATE TABLE customersPhone ( id SERIAL PRIMARY KEY, "customersId" INTEGER  NOT NULL,number VARCHAR(11) NOT NULL, type VARCHAR(20)); 
--CREATE TABLE bankAccount( id SERIAL PRIMARY KEY, "customerId" INTEGER NOT NULL, "accountNumber" VARCHAR(10) NOT NULL UNIQUE, agency VARCHAR(4), "openDate" timestamp NOT NULL NOW(), "closeDate" timestamp DEFAULT NULL);
--CREATE TABLE transactions (id SERIAL PRIMARY KEY, "bankAccountId" INTEGER NOT NULL, amount REAL NOT NULL, type TEXT NOT NULL, time TIMESTAMP WITHOUT TIME ZONE NOT NULL DEFAULT NOW(), description TEXT, cancelled BOOLEAN);
